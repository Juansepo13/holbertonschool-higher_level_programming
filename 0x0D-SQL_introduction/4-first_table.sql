-- Creates table called first_table in the current database in a MySQL server.
-- CREATE TABLE statement creates a table with the given name. Default database.
CREATE TABLE IF NOT EXISTS first_table(id INT, name VARCHAR(256));
